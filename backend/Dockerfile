# Використовуємо офіційний образ Go як базовий
FROM golang:1.19-alpine

# Встановлюємо робочий каталог для додатку
WORKDIR /app

# Копіюємо файли Go модуля
COPY go.mod go.sum ./

# Завантажуємо залежності
RUN go mod download

# Копіюємо решту файлів додатку
COPY . .

# Компілюємо додаток
RUN go build -o main .

# Запускаємо скомпільований додаток
CMD ["./main"]